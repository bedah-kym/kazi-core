[project]
name = "kazi-core"
version = "0.4.2"
description = "Self-hostable Django + Channels agentic engine — ReAct agent loop, workflow planning, pluggable connector registry."
readme = "README.md"
requires-python = ">=3.11,<3.13"
license = { text = "MIT" }
