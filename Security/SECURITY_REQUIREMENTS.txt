# Security-focused additions to requirements.txt
# Add these to your existing requirements.txt

# === ENHANCED SECURITY PACKAGES ===

# CORS handling (already configured, install if missing)
django-cors-headers>=3.14.0

# Additional security headers
django-csp>=3.7

# Password strength validation
django-password-validators>=1.7.0

# API rate limiting (enhance existing throttling)
djangorestframework-throttling>=0.1.0

# Secrets management (optional but recommended)
python-dotenv>=0.21.0

# Enhanced authentication
django-axes>=5.40.0  # Brute force protection (already in use)

# Audit logging
django-auditlog>=3.0.0

# Security scanning
bandit>=1.7.4  # Code security scanner

# Dependency vulnerability checking
safety>=2.3.0  # Check for known vulnerabilities
pip-audit>=2.4.0  # Audit packages

# === CRYPTOGRAPHY ===

# Already likely installed, but ensure these versions:
cryptography>=40.0.0

# Additional encryption utilities
pycryptodome>=3.17.0

# === MONITORING & LOGGING ===

# Structured logging
python-json-logger>=2.0.0

# Sentry for error tracking (optional)
sentry-sdk>=1.25.0

# === TESTING ===

# Security testing
factory-boy>=3.2.0  # Test data generation
pytest-django>=4.5.0  # Django testing
pytest-cov>=4.0.0  # Coverage reports

# === OPTIONAL BUT RECOMMENDED ===

# API schema validation
drf-spectacular>=0.26.0

# Request validation
marshmallow>=3.19.0

# IP address management
django-ipware>=4.0.0

# Rate limiting (alternative to throttling)
django-ratelimit>=4.0.0

# Webhook signature verification
hmac>=1.0  # Standard library, but explicitly list

# Time library improvements
python-dateutil>=2.8.0

# === DEVELOPMENT ONLY ===

# Code quality
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0

# Security in development
django-debug-toolbar>=3.8.0  # ONLY in development!

# Type checking
mypy>=1.0.0
django-stubs>=1.14.0

# Documentation
Sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0
